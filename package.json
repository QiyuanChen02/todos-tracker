{
	"name": "pretty-todo-kanban",
	"displayName": "Pretty Todo Kanban",
	"description": "Tracks your VSCode todo items and displays them in a pretty kanban board",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.104.0"
	},
	"type": "module",
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"main": "./out/extension.js",
	"icon": "logo.png",
	"publisher": "qiyuanchen",
	"repository": {
		"type": "git",
		"url": "https://github.com/QiyuanChen02/todos-tracker.git"
	},
	"contributes": {
		"commands": [
			{
				"command": "todos-tracker.openTodos",
				"title": "Open Todos"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "yarn run compile && yarn run build-webview",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"upgrade-wrpc": "yarn up @webview-rpc/host && cd webview && yarn up @webview-rpc/client @webview-rpc/react-query && cd ..",
		"build-webview": "cd webview && yarn build && cd .."
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.6",
		"@types/mocha": "^10.0.10",
		"@types/node": "22.x",
		"@types/vscode": "^1.104.0",
		"@vscode/test-cli": "^0.0.11",
		"@vscode/test-electron": "^2.5.2",
		"eslint": "^9.38.0",
		"typescript": "^5.9.3"
	},
	"packageManager": "yarn@1.22.22",
	"dependencies": {
		"@vscode/codicons": "^0.0.41",
		"@webview-rpc/host": "^1.0.17",
		"zod": "^4.1.12"
	}
}
